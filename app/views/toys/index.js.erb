// generate *JavaScript* that would be executed in the browser
console.log('this is a test')

// function refreshForm(innerHTML) {
//   const newReviewForm = document.getElementById('filter-form');
//   newReviewForm.innerHTML = innerHTML;
// }

// refreshForm('<%= j render "toys", @toys = Toy.all %>');
